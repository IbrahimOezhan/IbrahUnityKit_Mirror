using IbrahKit.UI;
using UnityEngine;

namespace IbrahKit
{
    public class Debug_UI_Menu : UI_Menu
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
