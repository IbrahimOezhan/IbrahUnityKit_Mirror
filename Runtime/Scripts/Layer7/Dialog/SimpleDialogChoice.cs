using UnityEngine;

public class SimpleDialogChoice : Dialog_Choice<SimpleDialogNode, SimpleDialogElement, SimpleDialogChoice>
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
